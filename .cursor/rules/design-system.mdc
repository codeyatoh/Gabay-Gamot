---
description: Extremely strict UI/UX design philosophy, colors, and styling rules
globs: *.jsx, *.js, *.tsx, *.ts, *.css
---
# Extremely Strict Design Philosophy
AESTHETICS ARE VERY IMPORTANT. If the UI looks simple, basic, or like a generic SaaS template, you have FAILED.
- **Color Palette:** Focus on clean, modern healthcare aesthetics.
  - Base: Soft creams, whites, or extremely light slate/zinc (`#ffffff`, `#fcfdfa`, `slate-50`).
  - Primary Accent: Medical Greens (`#0b6b35`, `#083f24`).
  - Borders: Very subtle and soft (`border-slate-100`, `border-[#e5f0e1]`).
- **Readability Over Effects:** **DO NOT** use heavy glassmorphism (`bg-white/50 backdrop-blur`) if it causes background text to bleed through and ruin readability. Use solid or highly opaque backgrounds (`bg-white/95 backdrop-blur-md`) for overlays.
- **Premium Components:** Customize Shadcn UI primitives extensively. Do not just copy-paste default cards or alerts. Add subtle shadows (`shadow-sm`, `shadow-panel`), rounded corners (`rounded-xl`, `rounded-2xl`), and elegant padding.
- **Animations:** Keep them calm, smooth, and purposeful.
  - Interactions: under 300ms (`transition duration-150`).
  - Reveals: 500ms-700ms (`fade-up`, `ease-out`).
  - **NO bouncy or flashy effects.**
- **Responsive System:** Build from 320px upward. Use `w-full`, `max-w-screen-*`, `section-shell`, responsive grid/flex wrapping, and `minmax(0,...)` for shrink-safe custom grid tracks.
- **No Horizontal Bleed:** Never introduce accidental horizontal scroll. Be careful with fixed/absolute elements, negative margins, `w-screen`, and large decorative layers.
- **Touch Native:** Interactive elements must feel good on touch devices: at least `min-h-11 min-w-11`, `cursor-pointer`, `transition-all`, and `active:scale-95` unless a component has a specific reason not to.
- **PWA Ready:** Respect safe areas using `pt-safe`, `pb-safe`, `px-safe`, or `env(safe-area-inset-*)`. Keep scrollbars visually hidden but scrolling functional.
- **Typography Fit:** Use responsive Tailwind text utilities. Do not use viewport-width font sizing. Text must not overflow compact UI surfaces.
- **Browser Verification:** Use Playwright screenshots or snapshots for significant visual changes, especially at 320px, 390px, 768px, 1024px, and 1440px widths.
